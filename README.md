# Arf

Arf is an experimental standalone AUR helper with an fzf interface.

Installation:
```sh
sudo pacman -S --needed base-devel git
git clone https://aur.archlinux.org/arf-git.git
cd arf-git
makepkg -si
```

For usage run `arf --help`.
